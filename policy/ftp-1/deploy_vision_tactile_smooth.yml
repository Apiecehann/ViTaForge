policy_name: ftp-1
seed: 0
instruction_type: seen
instruction_file: null
eval_from_start: true
eval_step_timeout_seconds: 5

ftp_1:
  host: "10.176.42.49"
  port: 8000
  api_key:
  prompt: "Pick up the USB plug from the blue slot and insert it into the red USB slot."
  prompt_from_task_instruction: true
  action_schema: "absolute_qpos8"
  action_dim: 8
  execute_from_index: 1

  # With temporal_ensemble=true, open_loop_horizon is the query interval:
  # 1 = query the FTP-1 server every env action step.
  open_loop_horizon: 1
  temporal_ensemble: true
  temporal_ensemble_horizon: 20
  temporal_ensemble_k: 0.01

  tactile_image_key:
  debug_dump_first_n_obs: 1
  debug_dump_dir: "debug/ftp_1_obs_vision_tactile_smooth"
  reconnect_sleep_s: 1
  request_retries: 1
  websocket_open_timeout: 10
  websocket_close_timeout: 10
